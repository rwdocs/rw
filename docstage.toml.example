# Docstage Configuration
# Copy this file to docstage.toml and customize for your project.

# Server settings for the documentation server
[server]
host = "127.0.0.1"      # Host to bind to (default: 127.0.0.1)
port = 8080             # Port to bind to (default: 8080)

# Documentation settings
[docs]
source_dir = "docs"     # Directory containing markdown files (default: docs)
cache_dir = ".cache"    # Cache directory for rendered content (default: .cache)

# Diagram rendering via Kroki
[diagrams]
kroki_url = "https://kroki.io"     # Kroki server URL (required for diagram rendering)
include_dirs = ["."]               # Directories to search for !include files
config_file = "config.iuml"        # PlantUML config file name (searched in include_dirs)
dpi = 192                          # DPI for diagram rendering (default: 192 for retina)

# Confluence integration (for publishing to Confluence)
# Generate tokens using: docstage generate-tokens
[confluence]
base_url = "https://confluence.example.com"
access_token = "your-oauth-access-token"
access_secret = "your-oauth-access-secret"
consumer_key = "docstage"          # OAuth consumer key (default: docstage)

# Test settings for Confluence commands
[confluence.test]
space_key = "DOCS"                 # Default space key for test commands
