- name: Enable Corepack
  run: corepack enable
- name: Install Yarn
  run: corepack install
- name: Build frontend
  run: corepack yarn install --immutable && corepack yarn workspace @rwdocs/viewer run build
