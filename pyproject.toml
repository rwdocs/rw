[project]
name = "md2conf-workspace"
version = "0.1.0"
description = "Markdown to Confluence workspace"
requires-python = ">=3.14"
# Workspace root is not a package itself
dependencies = []

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
md2conf-core = { workspace = true }
