# Leaf UnbreakableLeafIdentifierOfConsiderableLength entry

A leaf whose title carries a long unbreakable token, so its nav row uses the
22px spacer rather than a chevron button.

## Section UnbreakableConfigurationProviderIdentifierOfConsiderableLength details

The outline entry for this heading is wider than the 320px outline column
unless it breaks inside the token.
